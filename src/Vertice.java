public class Vertice {
    private int id;  // ID para identificar cada nodo(Ya sea escuela o gimnasio)
    public Vertice(int id) {
        this.id = id;
    }
    public int getId() {
        return id;
    }
}
